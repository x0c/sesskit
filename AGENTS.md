# SessKit

Read, parse, and export local coding-agent session history into one schema.

## Documentation navigation

- `README.md` / `README.zh-CN.md`: must read before changing public CLI/API surface or open-source facade.
- `schemas/*.json`: must read before changing JSON envelope fields or transcript event types — breaking the contract breaks every language consumer.
- `docs/CONTRACT.md`: must read before adding a runtime parser or changing status/event enums.

## Remote

- GitHub (public): `https://github.com/x0c/sesskit`
